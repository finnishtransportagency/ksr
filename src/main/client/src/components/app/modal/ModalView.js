// @flow
import React, { Fragment } from 'react';

type Props = {
    activeModal: string,
};

const ModalView = ({ activeModal }: Props) => (
    <Fragment>
        {/* modals */}
    </Fragment>
);

export default ModalView;
