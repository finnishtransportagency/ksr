import styled from 'styled-components';
import * as styles from '../../../ui/defaultStyles';

export const WorkspacenameInputWrapper = styled.div`
    .fa-exclamation-triangle {
        color: ${styles.mapHighlightStroke}
    }
`;
