import styled from 'styled-components';

const CustomTableBodyWrapper = styled.div`
    .ReactTable .tbody-scroll-wrapper {
        overflow-x: hidden !important;
        margin-bottom: 0 !important;
    }
`;

export default CustomTableBodyWrapper;
