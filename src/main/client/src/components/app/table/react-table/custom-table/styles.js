import styled from 'styled-components';

const CustomTableWrapper = styled.div`
    .ReactTable .rtable-scroll-wrapper {
        overflow-y: hidden !important;
        margin-right: 0 !important;
    }
`;

export default CustomTableWrapper;
