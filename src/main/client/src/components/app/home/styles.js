import styled from 'styled-components';
import * as styles from '../../ui/defaultStyles';

export const Wrapper = styled.div`
    text-align: center;
    border-top: 10px solid ${styles.colorMain};
    
    h1 {
        text-transform: uppercase;
    }
`;
