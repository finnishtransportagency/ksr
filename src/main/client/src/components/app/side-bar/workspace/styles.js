import styled from 'styled-components';

export const WorkspaceWrapper = styled.div`
    padding: 0 1em;

    button {
        width: 100%;
    }
    
    .Select-control {
        border-radius: 0;
    }
`;
