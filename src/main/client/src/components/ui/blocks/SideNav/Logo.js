import styled from 'styled-components';

const Logo = styled.div`
    height: 60px;
    width: 60px;
    display: flex;
    align-items: center;
    justify-content: center;
    
    img {
        height: 40px;
        width: 40px;
        z-index: 1;
    }
`;

export default Logo;
