import styled from 'styled-components';

const RemoveIcon = styled.div`
    flex: 1;
    align-self: center;
    text-align: right;

    & > i.fas.fa-trash {
        cursor: pointer;
    }
`;

export default RemoveIcon;
