import styled from 'styled-components';

const Span = styled.span`
    flex: 9;
    align-self: center;
    text-align: left;
    padding: 1rem;
    overflow: hidden;
    text-overflow: ellipsis;
`;

export default Span;
