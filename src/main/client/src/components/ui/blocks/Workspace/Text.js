import styled from 'styled-components';

const Text = styled.div`
    justify-self: center;
    align-self: center;
    overflow: hidden;
    text-overflow: ellipsis;
    padding: 0.5rem;
`;

export default Text;
