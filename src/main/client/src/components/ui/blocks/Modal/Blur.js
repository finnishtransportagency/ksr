import styled from 'styled-components';

const Blur = styled.div`
    position: fixed;
    background: rgba(0, 0, 0, 0.5);
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    opacity: 0.5; 
`;

export default Blur;
