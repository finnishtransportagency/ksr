// @flow
/**
 * TODO: add save to argcis server
 * This operation adds features to the associated feature layer
 * @param data Object feature layer
 */

export const saveNewFeatureData = (data: Object) => {
    const arrayData = [];
    arrayData.push(data);
    console.log(arrayData);
};
